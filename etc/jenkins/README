Persistent Memory Development Kit Tests

Jenkinsfile's in this directory let you run a Jenkins pipeline jobs which
contain different kinds of https://github.com/pmem/pmdk tests configurations.

Tests excluded because of external bug's in kernel:
	vmmalloc_fork/TEST1
	vmmalloc_fork/TEST3
	pmempool_transform/TEST20
